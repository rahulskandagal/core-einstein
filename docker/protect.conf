# Never serve the SQL dump or the container scripts over HTTP
<Directory /var/www/html/database>
    Require all denied
</Directory>
<Directory /var/www/html/docker>
    Require all denied
</Directory>
